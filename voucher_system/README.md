# Payment Voucher

## Installation

```bash
pip install -r requirements.txt
```

> **Note:** Playwright requires two additional commands after pip install:
> ```bash
> playwright install chromium
> playwright install-deps chromium
> ```

